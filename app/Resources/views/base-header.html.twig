<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">



        <div class="navbar-header">
            <!-- ================================ -->
            <!-- Ensure collapsible menu for mobile devices -->
            <!-- ================================ -->
            <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>


            <!-- ================================ -->
            <!-- Display brand -->
            <!-- ================================ -->
            <a class="navbar-brand" href="{{ path('public-index') }}">
                <span style="font-weight: bold; color:#EEEEEE;">
                    DMARC Dashboard
                </span>
                {% if not is_granted("IS_AUTHENTICATED_FULLY") %}
                    - Visualize your DMARC alignment
                {% endif %}
            </a>
        </div>   <!-- // navbar-header -->




        {% if is_granted("IS_AUTHENTICATED_FULLY") %}
            {% include 'base-header-menu-user.html.twig' %}
        {% else %}
            {% include 'base-header-menu-unauth.html.twig' %}
        {% endif %}





    </div>   <!-- container-fluid -->
</div>   <!-- navbar -->
